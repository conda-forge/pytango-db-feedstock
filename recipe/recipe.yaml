schema_version: 1

context:
  name: pytango-db
  version: "0.8.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/pytango_db-${{ version }}.tar.gz
  sha256: 2e496dc62400f3aed69d1ad4fadb4fcb74b131740b926dc1151c61996a6e941c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - PyDatabaseds = databaseds.database:main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >=${{ python_min }}
    - pytango

tests:
  - python:
      imports:
        - databaseds
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - 3.*
  - script:
      - PyDatabaseds --help
    requirements:
      run:
        - python ${{ python_min }}.*

about:
  summary: Fork of PyTango's internal sqlite based database device.
  license: LGPL-3.0-or-later
  license_file: LICENSE.txt
  homepage: https://gitlab.com/tango-controls/incubator/pytango-db
  repository: https://gitlab.com/tango-controls/incubator/pytango-db

extra:
  recipe-maintainers:
    - beenje
